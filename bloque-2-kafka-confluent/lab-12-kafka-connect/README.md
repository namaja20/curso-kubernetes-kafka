# LAB 12 — Integración con Kafka Connect

> **Entorno.** Cluster Kafka y pod **`kafka-cli`** del Lab 5; Connect se despliega con imagen **`cp-kafka-connect`** (API REST, no `kafka-*` en `kafka-cli`). [Entorno de práctica: pod kafka-cli](../docs/entorno-practica-kafka-cli.md)

## Objetivo

Comprender la integración de fuentes de datos hacia Kafka con **Kafka Connect**.

## Ejecución (guía)

- Configurar conector **JDBC → Kafka** (Confluent).

## Qué se aprende

Uso de Kafka Connect en entorno Confluent.

## Material

*(Definición de conector y BBDD de ejemplo: pendiente de la capa Kafka/Connect.)*
